version: 2.1

jobs:
  build:
    machine:
      image: ubuntu-2204:current
      #docker_layer_caching: true
    resource_class: medium
    steps:
      - checkout
      - run: |
          sudo apt-get install -y docker-buildx-plugin
      - run: |
          echo "$GITHUB_ACCESS_KEY" | docker login -u seungjin --password-stdin ghcr.io
      - run: |
          docker build -f Fedora/39/Containerfile \
          -t ghcr.io/seungjin/mytoolbox:fedora ./Fedora/39/
      - run: |
          docker build -f Fedora/rawhile/Containerfile \
          -t ghcr.io/seungjin/mytoolbox:fedora ./Fedora/rawhide/
      - run: |
          docker push ghcr.io/seungjin/mytoolbox:fedora
      - run: |
          docker build -f Debian/12/Containerfile \
          -t ghcr.io/seungjin/mytoolbox:debian ./Debian/12/
      - run: |
          docker build -f Debian/testing/Containerfile \
          -t ghcr.io/seungjin/mytoolbox:debian ./Debian/testing/
      - run: |
          docker build -f Debian/experimental/Containerfile \
          -t ghcr.io/seungjin/mytoolbox:debian ./Debian/experimental/
      - run: |
          docker push ghcr.io/seungjin/mytoolbox:debian
      #- run: |
      #    docker buildx imagetools create \
      #    -t ghcr.io/seungjin/mytoolbox:latest \
      #    ghcr.io/seungjin/mytoolbox:<<pipeline.parameters.image-tag>>

workflows:
  commit:
    jobs:
      - build
  daily:
    triggers:
      - schedule:
          cron: "30 18 * * *"
          filters:
            branches:
              only:
                - main
    jobs:
      - build

